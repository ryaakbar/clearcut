# ClearCut Environment Variables
# Copy this file to .env.local for local development

# Optional: Set to "production" in Vercel env settings
NODE_ENV=development

# Optional: Custom rate limit (requests per minute per IP)
RATE_LIMIT_MAX=10

# Optional: Max file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760
